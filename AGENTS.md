# AGENTS.md

## 프로젝트 목적

이 프로젝트는 범석과 세연이 각자 입력한 자산을 합산하여 관리하는 개인용 자산 포트폴리오 관리 앱이다.

핵심 목적은 다음과 같다.

1. 현재 자산의 자산군별 금액, 비중, 구성을 한눈에 확인한다.
2. 현재 비중과 목표 비중을 비교하여 리밸런싱 필요 금액을 확인한다.
3. 자산 스냅샷을 저장하고 전체 자산 및 카테고리별 추이를 확인한다.
4. 범석과 세연의 자산을 각각 관리하면서 기본적으로 합산 포트폴리오를 제공한다.

## 기술 스택과 구조

- React 18 + TypeScript
- Vite 5
- Tailwind CSS 3 + PostCSS + Autoprefixer
- Recharts
- 브라우저 LocalStorage 기반 상태 및 스냅샷 저장
- `html2canvas`를 이용한 이미지 내보내기
- `lucide-react` 아이콘
- 가격 공급자 추상화: mock provider와 real provider
- 실시간 가격 소스: Yahoo Finance(Vite 개발 서버 프록시), Upbit
- 경로 별칭: `@/*` -> `src/*`

주요 코드 위치:

- `src/types`: 도메인 타입과 저장 데이터 모델
- `src/data`: 기본 자산 및 종목 설정
- `src/hooks`: 포트폴리오 상태, 가격 조회, 이력 폴더 처리
- `src/services`: LocalStorage와 가격 공급자
- `src/utils`: 포트폴리오 계산, 리밸런싱, 포맷, 내보내기
- `src/pages`, `src/components`: 화면과 UI 컴포넌트

## 최우선 보존 원칙

- 기존 기능과 UI/UX를 유지한다.
- 최소 변경을 우선하고 관련 없는 파일은 수정하지 않는다.
- 정상 동작하는 코드를 단순히 더 나은 구조라는 이유로 리팩터링하지 않는다.
- 사용자 요청 없이 기능을 추가하거나 의존성을 교체하지 않는다.
- 사용자 요청 없이 아키텍처, 데이터 모델, 라우팅/탭 구조를 변경하지 않는다.
- 화면 디자인, 레이아웃, 색상, Tailwind 클래스, 컴포넌트 구성, interaction은 명시적인 요청이 있을 때만 변경한다.
- LocalStorage 저장 구조와 기존 저장 데이터의 backward compatibility를 유지한다.
- 자산 카테고리, 목표 비중, 리밸런싱, 차트, 기본 종목 설정을 임의로 변경하지 않는다.
- TypeScript 타입 안정성과 기존 코드 스타일을 유지한다.
- 계산 로직은 UI 로직과 분리하여 `utils` 또는 `services`에 둔다.
- 요구사항이 불명확하더라도 임의의 큰 구조 변경을 하지 않는다.
- Git commit 또는 push는 사용자가 명시적으로 요청했을 때만 수행한다.

## 사용자와 자산 카테고리

자산 소유자는 `beomseok`(범석)과 `seyeon`(세연)이다. 두 사람의 입력값은 각각 보존하며, 기본 대시보드는 합산 기준이다. 화면에서 범석, 세연, 합산 보기를 선택할 수 있다.

최상위 자산 카테고리는 다음 5개이며 이 구조를 유지한다.

1. 현금(`cash`)
2. 주식(`stock`)
3. 금(`gold`)
4. 가상화폐(`crypto`)
5. 고정자산(`fixedAsset`)

### 현금

- 유동 현금: 은행 계좌, 증권계좌 예수금 등 투자에 사용할 수 있는 돈
- 비유동 현금: 청약통장, 청년도약계좌 등 유동성이 낮은 돈
- 금액을 수기로 입력하며 입력 단위는 만원이다. 예: `2000`은 2,000만원이다.

### 주식

- 국내주식과 미국주식으로 구분한다.
- 종목별 보유 수량을 입력한다.
- 국내주식 평가액: `보유 수량 x 현재가(KRW)`
- 미국주식 평가액: `보유 수량 x 현재가(USD) x USD/KRW 환율`
- 종목 추가, 삭제, 수정 기능을 유지한다.
- 기본 국내 종목: SK하이닉스, 미코, 삼성전자, 파두
- 기본 미국 종목: TSLA, SPCX, RKLB, QLD, MRVL, IREN, INTC, INFQ, CRCL, CBRS

### 금

- 별도 하위 카테고리가 없다.
- KRX 금현물 기준으로 관리하며 보유량은 g 단위로 입력한다.
- 평가액: `보유 g x g당 원화 가격`
- 현재 real provider는 국제 금 선물 가격과 USD/KRW 환율을 이용해 g당 원화 가격으로 환산하며, 조회 실패 시 수동 설정 가격을 fallback으로 사용한다.

### 가상화폐

- Upbit의 원화 가격을 기준으로 관리한다.
- 종목별 보유 수량을 입력하며 소수 수량을 지원한다.
- 평가액: `보유 수량 x 현재 원화 가격`
- 종목 추가, 삭제, 수정 기능을 유지한다.
- 기본 종목: BTC, ETH, SOL, DOGE

### 고정자산

- 전세자금과 개인연금으로 구분한다.
- 금액을 수기로 입력하며 입력 단위는 만원이다.
- 범석과 세연 각각에 대해 전세자금과 개인연금의 포트폴리오 포함 여부를 선택할 수 있다.
- 선택된 고정자산만 목표 비중과 포트폴리오 총액 계산에 포함한다. 전체 고정자산을 포함한 총액도 기존 계산 방식대로 유지한다.

## 계산 및 표시 규칙

- 현금과 고정자산 입력값은 만원 단위지만 내부 계산은 모두 원 단위로 통일한다.
- 화면 금액은 원, 만원, 억원 단위로 읽기 쉽게 표시한다.
- 자산 평가와 합산은 `src/utils/portfolio.ts`의 기존 계산 방식을 기준으로 한다.
- 가격의 통화와 단위를 확인한 뒤 계산하며 국내주식, 미국주식, 금, 가상화폐의 평가 공식을 섞지 않는다.

## 목표 비중 규칙

- 최상위 5개 카테고리별 목표 비중을 설정한다.
- 고정자산은 포함 설정에 따라 목표 비중 계산 대상에 포함하거나 제외한다.
- 하위 목표 비중은 주식 종목과 가상화폐 종목에 대해서만 설정한다.
- 종목의 최종 포트폴리오 목표 비중은 `상위 카테고리 목표 비중 x 해당 카테고리 내 종목 목표 비중`이다.
- 최상위 목표 비중 합계는 100%인지 검증한다.
- 주식 내 종목별 목표 비중 합계와 가상화폐 내 종목별 목표 비중 합계도 각각 100%인지 검증한다.
- 합계가 100%가 아니면 기존 방식대로 사용자에게 경고한다.

예: 주식 목표 50%, 삼성전자 주식 내 목표 20%이면 삼성전자의 전체 포트폴리오 목표 비중은 10%이다.

## 리밸런싱 규칙

상위 카테고리와 주식/가상화폐 종목별로 다음 값을 제공한다.

- 현재 평가액
- 현재 비중
- 목표 비중
- 비중 차이
- 목표 금액
- 금액 차이

계산식은 다음과 같다.

- 비중 차이: `현재 비중 - 목표 비중`
- 목표 금액: `포트폴리오 총액 x 목표 비중`
- 금액 차이: `현재 평가액 - 목표 금액`
- 금액 차이가 양수이면 초과 보유, 음수이면 추가 매수 필요로 해석한다.

`src/utils/rebalancing.ts`의 부호와 계산 방향을 임의로 반전하지 않는다.

## 가격 조회와 실패 처리

- 가격 조회는 `PriceProvider` 인터페이스 뒤에 유지하여 UI와 분리한다.
- 설정에 따라 mock 또는 real provider를 사용한다.
- real provider는 Yahoo Finance에서 환율, 주식, 금 가격을 조회하고 Upbit에서 가상화폐 가격을 조회한다.
- 가격 캐시는 현재 구현의 TTL과 마지막 값 사용 방식을 유지한다.
- 조회 실패가 전체 포트폴리오 계산을 중단시키지 않도록 마지막 캐시 가격, 수동 입력 가격 또는 기존 fallback 값을 사용하고 오류 상태를 표시한다.
- 가격 API나 provider 구조를 변경할 때는 기존 통화 단위와 fallback 동작을 먼저 확인한다.

## 스냅샷과 이력

스냅샷 저장 시점에 다음 정보를 보존한다.

- 저장 날짜와 시간
- 범석/세연별 입력값
- 가격 정보
- 계산된 평가액과 총자산
- 카테고리별 평가액
- 당시 목표 비중 설정값

저장된 스냅샷으로 총자산 및 카테고리별 추이 차트를 구성한다. 기존 스냅샷을 읽지 못하게 하는 저장 형식 변경은 금지하며, 형식 변경이 명시적으로 요청된 경우 반드시 migration과 backward compatibility를 함께 설계한다.

## 화면과 기능 범위

현재 탭 구조를 유지한다.

1. 대시보드: 총자산, 카테고리별 금액/비중, 파이차트, 목표 대비 차이 요약
2. 자산 입력: 사용자 선택과 현금, 주식, 금, 가상화폐, 고정자산 입력
3. 목표 비중 설정: 최상위 및 종목별 목표 비중과 합계 검증
4. 리밸런싱: 현재/목표 비중과 초과/부족 금액 표
5. 이력: 스냅샷 목록과 자산 추이 차트
6. 설정: 종목 관리, 고정자산 포함 설정, 가격 provider 설정

파이차트, 자산 표, 이력 라인차트 및 내보내기 동작을 변경할 때는 명시적 사용자 요청이 필요하다.

## Codex 작업 순서

1. 요청과 관련된 코드, 타입, 데이터 흐름을 탐색한다.
2. 기존 동작과 저장 호환성, 영향을 받는 화면을 파악한다.
3. 변경 범위를 필요한 파일과 동작으로 최소화한다.
4. 기존 패턴과 스타일에 맞춰 구현한다.
5. 프로젝트에 실제 존재하는 typecheck, build, test, lint 명령만 수행한다.
6. diff를 검토하여 관련 없는 UI, CSS, 계산, 데이터 모델, 의존성 변경이 없는지 확인한다.
7. 변경사항, 검증 결과, 남은 regression 가능성을 요약한다.

현재 `package.json`에서 제공하는 명령은 다음과 같다.

- 개발 서버: `npm run dev`
- TypeScript 검사 + 프로덕션 빌드: `npm run build`
- 빌드 미리보기: `npm run preview`

별도 test 및 lint 스크립트는 현재 없다. 검증 명령을 임의로 추가하지 않는다. Windows PowerShell 실행 정책으로 `npm.ps1` 실행이 차단되면 동일한 npm CLI인 `npm.cmd`를 사용한다.
